import { ReactElement, ReactNode } from 'react';
import { ToastEnterEvent, ToastOptions } from '../type/common';
export interface ToastMessageProps extends Pick<ToastOptions, 'className' | 'clickable' | 'position' | 'render' | 'theme' | 'onClick'> {
    id: number;
    message: ReactNode;
    isExit?: boolean;
    offsetX?: string;
    offsetY?: string;
    baseOffsetX?: number;
    baseOffsetY?: number;
    zIndex?: number;
    loading?: boolean | Promise<unknown>;
    _onEnter?: (e: ToastEnterEvent) => void;
}
declare function ToastMessage({ id, message, className, clickable, position, isExit, render, theme, offsetX, offsetY, baseOffsetX, baseOffsetY, zIndex, loading, onClick, _onEnter, }: ToastMessageProps): ReactElement;
export default ToastMessage;
